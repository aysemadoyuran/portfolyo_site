@model entitylayer.Concrete.service
@{
    ViewData["Title"] = "EditService";
    Layout = "~/Views/Shared/_NewAdminLayout.cshtml";
}

@*<div class="col-xl-12 order-xl-1">
        <div class="card">
            <div class="card-header">
                <div class="row align-items-center">
                    <div class="col-8">
                        <h3 class="mb-0">Hizmet Güncelle </h3>
                    </div>

                </div>
            </div>
            @using (Html.BeginForm("EditService", "Service", FormMethod.Post))
            {
                @Html.HiddenFor(x => x.ServiceID)
                <div class="card-body">
                    <h6 class="heading-small text-muted mb-4">Hizmet Bilgileri</h6>
                    <div class="pl-lg-4">
                        <div class="row">
                            <div class="col-lg-12">
                                <div class="form-group">
                                    <label class="form-control-label">Yetenek Adı:</label>
                                    @Html.TextBoxFor(x => x.Title, new { @class = "form-control" })
                                </div>
                            </div>
                            <div class="col-lg-12">
                                <div class="form-group">
                                    <label class="form-control-label">Yetenek Oranı</label>
                                    @Html.TextBoxFor(x => x.ImageUrl, new { @class = "form-control" })
                                </div>
                            </div>

                            <div class="col-lg-12">
                                <div class="d-flex justify-content-end">
                                    <button class="btn btn-info ">Hizmet Güncelle</button>
                                </div>

                            </div>

                        </div>

                    </div>
                </div>
            }



        </div>
    </div>*@
<div class="col-md-12 grid-margin stretch-card">
    <div class="card">
        <div class="card-body">
            <h4 class="card-title">Hizmet Güncelle</h4>
            <p class="card-description"> Deneyim Bilgileri </p>
            @using (Html.BeginForm("EditService", "Service", FormMethod.Post))
            {
                @Html.HiddenFor(x => x.ServiceID)
                <div class="form-group">
                    <label class="form-control-label">Yetenek Adı:</label>
                    @Html.TextBoxFor(x => x.Title, new { @class = "form-control" })
                </div>
                <br />
                <div class="form-group">
                    <label class="form-control-label">Yetenek Oranı</label>
                    @Html.TextBoxFor(x => x.ImageUrl, new { @class = "form-control" })
                </div>
                <br />
                <div class="form-group">
                    <div class="d-flex justify-content-end">
                        <button class="btn btn-outline-success ">Deneyim Güncelle</button>
                    </div>
                </div>
                <br />

            }
        </div>
    </div>
</div>

